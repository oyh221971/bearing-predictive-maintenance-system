# 安全策略（Security Policy）

## 支持版本

| 版本 | 支持状态 |
| --- | --- |
| 最新 main 分支 | ✅ 持续维护 |

## 报告安全漏洞

如果你发现安全漏洞，请通过 GitHub Issues 报告（标注 `[安全]`），我们会尽快响应。
**请勿在公开 Issue 中泄露可被利用的细节或真实攻击数据。**

## 本项目的安全承诺

- **零密钥入库**：本仓库不包含任何 API 密钥、访问令牌、密码或私钥。
  项目不依赖任何外部大模型/云服务 API，全部功能本地运行。
- **默认最小暴露**：服务默认仅监听 `127.0.0.1:8000`（仅本机可访问），
  不开启鉴权，请勿在未加固的情况下直接暴露到公网。
- **无数据外传**：系统不采集、不上传任何遥测数据；所有记录仅存于本地 SQLite。

## 部署加固清单（公网/生产环境）

1. 修改 `app/config.py` 中 `HOST` 为内网地址而非 `0.0.0.0`，或用反向代理限流；
2. 在反向代理层（Nginx/Caddy）加 HTTPS 与访问鉴权（Basic Auth / OAuth）；
3. FastAPI 层可自行加认证中间件（本系统为演示设计，默认无登录功能）；
4. 生产环境建议将 SQLite 替换为 PostgreSQL 等并发数据库；
5. 定期更新依赖：`pip list --outdated`。

## 免责声明

本系统为研究与教学用途的**演示性预测性维护工具**，诊断结果不构成安全关键
决策依据，不应用于可能造成人身或财产损失的场景（详见 README「能力范围」）。
